# AI Cafe 구현 계획서

> **문서 버전:** 2.0.0
> **작성일:** 2026-01-24
> **상태:** 승인됨
> **변경 사항:** 단일 화면 키오스크 레이아웃으로 전면 개편

---

## 1. 프로젝트 개요

### 1.1 목표

Next.js 기반 AI 음성 인식 키오스크 시스템 개발

### 1.2 핵심 원칙

- **단일 화면**: 모든 정보가 한 화면에 표시 (페이지 전환 없음)
- **100% 브라우저 로컬**: 외부 API 없이 모든 AI 기능 브라우저에서 실행
- **키오스크 전용**: 1920x1080 가로 화면 최적화 (모바일 지원 제외)

### 1.3 핵심 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│           AI Cafe - 단일 화면 키오스크 플로우                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌───────────────────────────────────────────────────────────┐ │
│   │                    단일 화면 레이아웃                      │ │
│   │  ┌──────────────────────────────────────────────────────┐ │ │
│   │  │                    헤더 (AI 상태)                     │ │ │
│   │  ├────────────────────────┬─────────────────────────────┤ │ │
│   │  │     메뉴 영역 (65%)    │     주문 영역 (35%)         │ │ │
│   │  │                        │                             │ │ │
│   │  │  - 카테고리 탭         │  - 현재 주문 목록           │ │ │
│   │  │  - 메뉴 그리드         │  - 음성 주문 인터페이스     │ │ │
│   │  │                        │  - 총액 및 주문 버튼        │ │ │
│   │  ├────────────────────────┴─────────────────────────────┤ │ │
│   │  │              대기열 영역 (하단 고정)                  │ │ │
│   │  │  - 준비 중 (좌)       │  - 픽업 대기 (우)           │ │ │
│   │  └──────────────────────────────────────────────────────┘ │ │
│   └───────────────────────────────────────────────────────────┘ │
│                                                                  │
│   ┌───────────────────────────────────────────────────────────┐ │
│   │                    상태 전환 (화면 이동 없음)              │ │
│   │                                                           │ │
│   │   [대기 중] ─얼굴감지→ [인사] ─자동→ [주문 가능]          │ │
│   │       │                                                   │ │
│   │       └─버튼클릭→ [주문 가능]                             │ │
│   │                       │                                   │ │
│   │                 음성/터치 주문                            │ │
│   │                       │                                   │ │
│   │               [주문 확인 중] ─확정→ [대기열 등록]         │ │
│   │                       │                                   │ │
│   │                 수정 (같은 화면에서)                      │ │
│   │                                                           │ │
│   └───────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 전문가별 의견 종합

### 2.1 프론트엔드 아키텍트 관점

**단일 화면 아키텍처:**

```
src/
├── app/                      # Next.js App Router
│   ├── layout.tsx           # 루트 레이아웃 (COOP/COEP 메타)
│   ├── page.tsx             # 단일 페이지 (모든 기능 통합)
│   └── globals.css          # 전역 스타일
│
├── components/
│   ├── layout/              # 레이아웃 컴포넌트
│   │   ├── Header.tsx       # AI 상태 + 로고
│   │   ├── MainContent.tsx  # 메뉴 + 주문 영역 래퍼
│   │   └── QueueSection.tsx # 하단 대기열 섹션
│   │
│   ├── menu/                # 메뉴 영역 컴포넌트
│   │   ├── MenuSection.tsx  # 메뉴 영역 컨테이너
│   │   ├── CategoryTabs.tsx # 카테고리 탭
│   │   ├── MenuGrid.tsx     # 메뉴 그리드
│   │   └── MenuItem.tsx     # 개별 메뉴 아이템
│   │
│   ├── order/               # 주문 영역 컴포넌트
│   │   ├── OrderSection.tsx # 주문 영역 컨테이너
│   │   ├── OrderList.tsx    # 주문 목록
│   │   ├── OrderItem.tsx    # 개별 주문 아이템
│   │   ├── VoiceOrder.tsx   # 음성 주문 인터페이스 (인라인)
│   │   ├── OrderSummary.tsx # 총액 표시
│   │   └── OrderActions.tsx # 주문/취소 버튼
│   │
│   ├── queue/               # 대기열 컴포넌트
│   │   ├── PreparingQueue.tsx # 준비 중 대기열
│   │   ├── ReadyQueue.tsx     # 픽업 대기열
│   │   └── QueueCard.tsx      # 대기열 카드
│   │
│   ├── ai/                  # AI 상태 컴포넌트
│   │   ├── AIStatusBar.tsx  # AI 모델 상태 표시
│   │   ├── AIStatusDot.tsx  # 개별 상태 점
│   │   └── WelcomeOverlay.tsx # 환영 오버레이 (얼굴 감지 시)
│   │
│   └── ui/                  # 기본 UI 컴포넌트
│       ├── Button.tsx
│       ├── Card.tsx
│       ├── Badge.tsx
│       └── WaveformIndicator.tsx
│
├── hooks/                   # Custom Hooks
│   ├── useKioskState.ts     # 키오스크 상태 머신
│   ├── useFaceDetection.ts
│   ├── useSpeechToText.ts
│   ├── useTextToSpeech.ts
│   ├── useLLM.ts
│   └── useAIStatus.ts
│
├── lib/                     # 유틸리티 & 서비스
│   ├── ai/
│   │   ├── face-detection.ts
│   │   ├── stt.ts
│   │   ├── tts.ts
│   │   ├── llm.ts
│   │   └── fallback.ts
│   ├── order/
│   │   ├── parser.ts
│   │   └── validator.ts
│   └── utils/
│       ├── audio.ts
│       └── browser-support.ts
│
├── workers/                 # Web Workers
│   ├── face-detection.worker.ts
│   ├── stt.worker.ts
│   ├── tts.worker.ts
│   └── llm.worker.ts
│
├── store/                   # 상태 관리 (Zustand)
│   ├── kioskStore.ts        # 키오스크 상태 (idle/active/ordering)
│   ├── orderStore.ts
│   ├── queueStore.ts
│   └── aiStore.ts
│
├── types/                   # TypeScript 타입
│   ├── menu.ts
│   ├── order.ts
│   ├── queue.ts
│   ├── kiosk.ts
│   └── ai.ts
│
└── data/                    # 정적 데이터
    └── menu.ts
```

**핵심 권장사항:**
1. **단일 페이지**: `app/page.tsx`에서 모든 섹션 렌더링
2. **상태 기반 UI**: 키오스크 상태(idle/active/ordering)에 따라 UI 변경
3. **Web Worker**: 모든 AI 모델을 별도 워커에서 실행
4. **CSS Grid**: 3단 레이아웃(헤더/메인/푸터)으로 구성

---

### 2.2 AI/ML 엔지니어 관점

**모델 로딩 전략:**

```typescript
// 단일 화면에서의 로딩 우선순위
const AI_LOAD_STRATEGY = {
  // Phase 1: 즉시 로드 (페이지 로드 시)
  immediate: ['faceDetection'],  // ~3MB, 2-3초

  // Phase 2: 유휴 시간에 로드
  idle: ['tts'],  // ~85MB, 5-10초 - 인사말에 필요

  // Phase 3: 사용자 감지 후 또는 버튼 클릭 시
  onInteraction: ['stt', 'llm'],  // STT ~250MB, LLM ~1GB
};
```

**Web Worker 아키텍처:**

```
┌─────────────────────────────────────────────────────────────────┐
│                         Main Thread                              │
├─────────────────────────────────────────────────────────────────┤
│  React Components (단일 페이지)                                   │
│  ├── Header → AIStatusBar                                        │
│  ├── MenuSection + OrderSection (동시 표시)                      │
│  └── QueueSection (항상 표시)                                    │
└────────────────────────┬────────────────────────────────────────┘
                         │ postMessage / onmessage
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Web Workers                               │
├─────────────────────────────────────────────────────────────────┤
│  face-detection.worker.ts                                        │
│  ├── TensorFlow.js + MediaPipe                                  │
│  └── 항상 실행 (대기 상태에서 고객 감지)                         │
│                                                                  │
│  stt.worker.ts                                                   │
│  ├── Transformers.js + Whisper                                  │
│  └── 음성 주문 버튼 클릭 시 활성화                               │
│                                                                  │
│  tts.worker.ts                                                   │
│  ├── Supertonic TTS                                                  │
│  └── 인사말/주문확인/픽업호출 시 사용                            │
│                                                                  │
│  llm.worker.ts                                                   │
│  ├── WebLLM + Qwen                                              │
│  └── 음성 인식 후 주문 파싱                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

### 2.3 UX 디자이너 관점

**단일 화면 레이아웃 (1920x1080):**

```
┌────────────────────────────────────────────────────────────────────────────────┐
│                              AI Cafe  ☕                                     │
│  🟢 얼굴인식  🟢 음성인식  🟢 음성합성  🟢 AI처리                [60px 헤더]   │
├────────────────────────────────────────────────────┬───────────────────────────┤
│                                                    │                           │
│  [커피] [논커피] [디저트] [시즌]                    │   📋 현재 주문            │
│                                                    │                           │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐     │   아이스 아메리카노       │
│  │  ☕  │ │  ☕  │ │  ☕  │ │  🍵  │ │  🍵  │     │   ├─ ICE │ ➖ 2 ➕        │
│  │아메리│ │카페  │ │바닐라│ │녹차  │ │초코  │     │   └─ 9,000원             │
│  │카노  │ │라떼  │ │라떼  │ │라떼  │ │라떼  │     │                           │
│  │4,500 │ │5,000 │ │5,500 │ │5,500 │ │5,500 │     │   카페라떼                │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘     │   ├─ HOT │ ➖ 1 ➕        │
│                                                    │   └─ 5,000원             │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐     │   ──────────────────      │
│  │  ☕  │ │  ☕  │ │  ☕  │ │  🍵  │ │  🍵  │     │                           │
│  │카라멜│ │헤이즐│ │콜드  │ │말차  │ │밀크  │     │   🎤 음성으로 주문하기    │
│  │마키아│ │넛라떼│ │브루  │ │라떼  │ │티    │     │   "아이스 아메리카노      │
│  │5,500 │ │5,500 │ │5,000 │ │6,000 │ │5,000 │     │    두 잔 주세요"          │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘     │                           │
│                                                    │   ──────────────────      │
│                   [메뉴 영역 65%]                  │   총 14,000원             │
│                                                    │                           │
│                                                    │   [취소]    [주문하기]    │
│                                                    │                           │
│                                                    │   [주문 영역 35%]         │
├────────────────────────────────────────────────────┴───────────────────────────┤
│                                                                                │
│   📋 준비 중                              ✅ 픽업 대기                          │
│   ┌─────────┐ ┌─────────┐ ┌─────────┐    ┌─────────┐ ┌─────────┐              │
│   │  #1042  │ │  #1043  │ │  #1044  │    │  #1038  │ │  #1039  │   [100px]    │
│   │ 아메x2  │ │ 라떼x3  │ │ 바닐라  │    │ 라떼x1  │ │ 아메x1  │              │
│   └─────────┘ └─────────┘ └─────────┘    └─────────┘ └─────────┘              │
│                                                                                │
└────────────────────────────────────────────────────────────────────────────────┘
```

**상태별 UI 변화 (같은 화면에서):**

| 상태 | 메뉴 영역 | 주문 영역 | 대기열 영역 |
|------|----------|----------|------------|
| idle (대기) | 희미하게 표시 | 환영 메시지 + 시작 버튼 | 정상 표시 |
| active (활성) | 정상 표시 | 주문 목록 표시 | 정상 표시 |
| ordering (주문 중) | 정상 표시 | 음성 입력 활성화 | 정상 표시 |
| confirming (확인) | 정상 표시 | 확인 메시지 + 버튼 강조 | 정상 표시 |

**핵심 권장사항:**
1. 화면 전환 없이 상태에 따른 강조/희미 처리
2. 대기열은 항상 표시하여 전체 흐름 파악 가능
3. 음성 주문 UI는 주문 영역 내에서 인라인으로 표시
4. 오버레이는 환영 메시지에만 사용 (반투명, 터치로 닫기)

---

### 2.4 백엔드 아키텍트 관점

**키오스크 상태 관리:**

```typescript
// types/kiosk.ts
type KioskState =
  | 'idle'       // 대기 중 (고객 없음)
  | 'active'     // 활성 (고객 감지됨)
  | 'ordering'   // 주문 중 (음성/터치 입력)
  | 'confirming' // 주문 확인 중
  | 'processing' // 주문 처리 중

// store/kioskStore.ts
interface KioskStore {
  state: KioskState;
  lastActivity: Date;
  customerDetected: boolean;

  // Actions
  setIdle: () => void;
  setActive: () => void;
  startOrdering: () => void;
  confirmOrder: () => void;
  processOrder: () => void;

  // Auto-reset (30초 무활동 시 idle로)
  resetOnInactivity: () => void;
}
```

**데이터 모델:**

```typescript
// types/menu.ts
interface MenuItem {
  id: string;
  name: string;
  nameEn: string;
  category: 'coffee' | 'non-coffee' | 'dessert' | 'seasonal';
  price: number;
  temperatures: ('HOT' | 'ICE')[];
  image: string;
  description?: string;
  available: boolean;
}

// types/order.ts
interface OrderItem {
  menuId: string;
  name: string;
  temperature: 'HOT' | 'ICE';
  quantity: number;
  price: number;
  options?: OrderOption[];
}

interface Order {
  id: string;
  orderNumber: number;
  items: OrderItem[];
  total: number;
  status: 'pending' | 'confirmed' | 'preparing' | 'ready' | 'completed';
  createdAt: Date;
  updatedAt: Date;
}

// types/queue.ts
interface QueueItem {
  orderId: string;
  orderNumber: number;
  items: string[];
  status: 'preparing' | 'ready';
  estimatedTime?: number;
  createdAt: Date;
}
```

**상태 관리 (Zustand):**

```typescript
// store/orderStore.ts
interface OrderStore {
  currentOrder: OrderItem[];

  addItem: (item: OrderItem) => void;
  removeItem: (index: number) => void;
  updateQuantity: (index: number, quantity: number) => void;
  clearOrder: () => void;
  getTotal: () => number;
}

// store/queueStore.ts
interface QueueStore {
  preparingQueue: QueueItem[];
  readyQueue: QueueItem[];
  nextOrderNumber: number;

  addToQueue: (order: Order) => QueueItem;
  moveToReady: (orderId: string) => void;
  completeOrder: (orderId: string) => void;
  startSimulation: () => void;
}
```

---

### 2.5 DevOps 엔지니어 관점

**필수 헤더 설정:**

```javascript
// next.config.js
const nextConfig = {
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          {
            key: 'Cross-Origin-Opener-Policy',
            value: 'same-origin',
          },
          {
            key: 'Cross-Origin-Embedder-Policy',
            value: 'require-corp',
          },
        ],
      },
    ];
  },

  webpack: (config) => {
    config.experiments = {
      ...config.experiments,
      asyncWebAssembly: true,
    };
    return config;
  },
};
```

---

### 2.6 QA 엔지니어 관점

**단일 화면 테스트 시나리오:**

| ID | 시나리오 | 예상 결과 |
|----|---------|----------|
| TC-001 | 페이지 로드 | 모든 영역(헤더/메뉴/주문/대기열) 표시 |
| TC-002 | 얼굴 감지 | 환영 오버레이 표시, TTS 인사말 |
| TC-003 | 오버레이 터치 | 오버레이 닫힘, active 상태 전환 |
| TC-004 | 메뉴 터치 | 주문 영역에 아이템 추가 |
| TC-005 | 수량 조절 | 수량 증가/감소, 총액 업데이트 |
| TC-006 | 음성 주문 버튼 | 음성 입력 UI 활성화 |
| TC-007 | 음성 인식 완료 | LLM 파싱 → 주문 목록 업데이트 |
| TC-008 | 주문하기 클릭 | 확인 다이얼로그, TTS 확인 안내 |
| TC-009 | 주문 확정 | 대기열에 추가, 번호 안내 |
| TC-010 | 대기열 상태 변경 | 준비 중 → 픽업 대기 이동, TTS 호출 |
| TC-011 | 30초 무활동 | idle 상태 복귀 |

---

## 3. Feature 분해 (scope.yaml)

```yaml
project:
  name: ai-cafe
  type: greenfield
  description: AI 기반 음성 인식 키오스크 (단일 화면)

features:
  # Phase 1: 기반 구축
  - name: project-setup
    description: Next.js 프로젝트 초기 설정
    branch: feature/project-setup
    dependencies: []
    deliverables:
      - Next.js 14 프로젝트 생성
      - TypeScript, Tailwind CSS 설정
      - 폴더 구조 생성
      - COOP/COEP 헤더 설정
      - 단일 페이지 레이아웃 스켈레톤

  - name: design-system
    description: 디자인 토큰 및 기본 UI 컴포넌트
    branch: feature/design-system
    dependencies: [project-setup]
    deliverables:
      - 디자인 토큰 (색상, 타이포그래피, 간격)
      - Button, Card, Badge 컴포넌트
      - WaveformIndicator 컴포넌트
      - 키오스크용 터치 최적화 스타일

  # Phase 2: 단일 화면 레이아웃
  - name: kiosk-layout
    description: 단일 화면 레이아웃 구현
    branch: feature/kiosk-layout
    dependencies: [design-system]
    deliverables:
      - Header 컴포넌트
      - MainContent 래퍼 (65/35 분할)
      - QueueSection 컴포넌트
      - CSS Grid 기반 레이아웃
      - 반응형 (1920x1080 기준)

  - name: menu-section
    description: 메뉴 영역 구현
    branch: feature/menu-section
    dependencies: [kiosk-layout]
    deliverables:
      - 메뉴 데이터 구조 및 샘플 데이터
      - CategoryTabs 컴포넌트
      - MenuGrid 컴포넌트
      - MenuItem 컴포넌트

  - name: order-section
    description: 주문 영역 구현
    branch: feature/order-section
    dependencies: [kiosk-layout]
    deliverables:
      - Zustand 주문 스토어
      - OrderList 컴포넌트
      - OrderItem 컴포넌트
      - OrderSummary 컴포넌트
      - OrderActions 컴포넌트

  - name: queue-section
    description: 대기열 영역 구현
    branch: feature/queue-section
    dependencies: [kiosk-layout]
    deliverables:
      - Zustand 대기열 스토어
      - PreparingQueue 컴포넌트
      - ReadyQueue 컴포넌트
      - QueueCard 컴포넌트
      - 대기열 시뮬레이션

  # Phase 3: AI 통합
  - name: ai-infrastructure
    description: AI 모델 로딩 인프라
    branch: feature/ai-infrastructure
    dependencies: [project-setup]
    deliverables:
      - Web Worker 설정
      - AI 상태 스토어
      - 모델 로딩 우선순위 관리
      - Fallback 체인 구현
      - 브라우저 호환성 체크

  - name: face-detection
    description: 얼굴 감지 기능
    branch: feature/face-detection
    dependencies: [ai-infrastructure, kiosk-layout]
    deliverables:
      - MediaPipe + TensorFlow.js 통합
      - face-detection.worker.ts
      - useFaceDetection 훅
      - WelcomeOverlay 컴포넌트
      - 키오스크 상태 연동

  - name: speech-to-text
    description: 음성 인식 기능
    branch: feature/speech-to-text
    dependencies: [ai-infrastructure]
    deliverables:
      - Whisper (Transformers.js) 통합
      - stt.worker.ts
      - useSpeechToText 훅
      - VoiceOrder 컴포넌트 (인라인)
      - Web Speech API fallback

  - name: text-to-speech
    description: 음성 출력 기능
    branch: feature/text-to-speech
    dependencies: [ai-infrastructure]
    deliverables:
      - Supertonic TTS 통합
      - tts.worker.ts
      - useTextToSpeech 훅
      - 인사말/확인/호출 음성

  - name: llm-integration
    description: LLM 주문 처리 기능
    branch: feature/llm-integration
    dependencies: [ai-infrastructure, order-section]
    deliverables:
      - WebLLM (Qwen) 통합
      - llm.worker.ts
      - useLLM 훅
      - 주문 파싱 프롬프트
      - 규칙 기반 파싱 fallback

  # Phase 4: 통합 및 완성
  - name: kiosk-state-machine
    description: 키오스크 상태 머신
    branch: feature/kiosk-state-machine
    dependencies: [face-detection, order-section]
    deliverables:
      - Zustand 키오스크 스토어
      - useKioskState 훅
      - 상태 전환 로직
      - 무활동 타이머 (30초 리셋)

  - name: voice-order-flow
    description: 음성 주문 플로우 통합
    branch: feature/voice-order-flow
    dependencies: [speech-to-text, text-to-speech, llm-integration]
    deliverables:
      - STT → LLM → TTS 파이프라인
      - 주문 수정 음성 처리
      - 에러 처리 및 재시도

  - name: full-integration
    description: 전체 시스템 통합
    branch: feature/full-integration
    dependencies: [kiosk-state-machine, voice-order-flow, queue-section]
    deliverables:
      - AI 상태바 통합 (Header)
      - 전체 플로우 테스트
      - 데모 모드 (시뮬레이션)

review_wave:
  runs_after: all features merged
  tasks:
    - E2E 테스트
    - 성능 최적화
    - 버그 수정
```

---

## 4. 의존성 그래프

```
project-setup
     │
     ├──────────────────────────────┐
     ▼                              ▼
design-system              ai-infrastructure
     │                              │
     ▼                    ┌─────────┼─────────┐
kiosk-layout              ▼         ▼         ▼
     │              face-detection  stt       tts
     │                    │         │         │
     ├────────┬───────────┤         └────┬────┘
     ▼        ▼           ▼              │
menu-sect  order-sect  queue-sect       ▼
     │        │           │        llm-integration
     │        │           │              │
     │        └───────────┼──────────────┘
     │                    │
     │                    ▼
     │           kiosk-state-machine
     │                    │
     │                    │         voice-order-flow
     │                    │              │
     └────────────────────┴──────────────┘
                          │
                          ▼
                  full-integration
                          │
                          ▼
                    review-wave
```

---

## 5. 위험 요소 및 대응

### 5.1 기술적 위험

| 위험 | 영향도 | 발생 확률 | 대응 방안 |
|------|--------|----------|----------|
| WebGPU 미지원 | 높음 | 낮음 | WASM fallback |
| 한국어 STT 정확도 | 중간 | 중간 | Web Speech API + 터치 fallback |
| LLM 로딩 시간 | 중간 | 높음 | 프로그레시브 로딩 + 규칙 기반 fallback |
| 메모리 부족 | 높음 | 낮음 | 모델 크기 조정 (q4) |

### 5.2 Fallback 체인

```
Face Detection: MediaPipe → 수동 시작 버튼
STT: Whisper → Web Speech API → 터치 입력
TTS: Supertonic → Web Speech API
LLM: WebLLM (Qwen) → 규칙 기반 파싱
```

---

## 6. 성공 기준

### 6.1 기능 완성도

| 기능 | 필수 | 목표 |
|------|------|------|
| 단일 화면 레이아웃 | ✅ | 100% |
| 얼굴 감지 → 자동 인사 | ✅ | 90% 감지율 |
| 음성 주문 (한국어) | ✅ | 85% 인식률 |
| 터치 주문 | ✅ | 100% |
| 음성 안내 | ✅ | 자연스러운 음성 |
| LLM 주문 파싱 | ✅ | 90% 정확도 |
| 대기열 관리 | ✅ | 100% |

### 6.2 품질 기준

| 항목 | 기준 |
|------|------|
| 초기 로딩 | < 5초 (캐시 후) |
| AI 모델 로딩 | 프로그레시브 (UI 블로킹 없음) |
| 에러 처리 | 모든 실패에 fallback 동작 |
| 화면 해상도 | 1920x1080 최적화 |

---

## 7. 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 1.0.0 | 2026-01-24 | 초기 문서 작성 | AI Cafe Team |
| 2.0.0 | 2026-01-24 | 단일 화면 키오스크 레이아웃으로 전면 개편 | AI Cafe Team |
